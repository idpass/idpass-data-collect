# PostgreSQL Environment Configuration Example for HDM Sync
# Copy this file to postgresql.env and update with your values
# Make sure to use the same credentials as you use for the connection string in your .env file

POSTGRES_USER=your_postgres_user
POSTGRES_PASSWORD=your_postgres_password
POSTGRES_DB=datacollect
