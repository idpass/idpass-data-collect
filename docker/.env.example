# Sync Server Environment Configuration Example
# Copy this file to .env and update with your values

# Database configuration for Sync Server
# Make sure to use the same credentials in the postgres.env file
POSTGRES_USER=idpass_user
POSTGRES_PASSWORD=idpass_pass
POSTGRES_DB=datacollect
POSTGRES_HOST=postgres # Docker service name

POSTGRES=postgresql://$POSTGRES_USER:$POSTGRES_PASSWORD@$POSTGRES_HOST:5432/$POSTGRES_DB
POSTGRES_TEST=postgresql://$POSTGRES_USER:$POSTGRES_PASSWORD@$POSTGRES_HOST:5432/${POSTGRES_DB}_test

# Authentication
INITIAL_PASSWORD=changeme # Initial admin password
JWT_SECRET=your-secret-key-here # JWT secret for authentication

# Server configuration
SYNC_SERVER_PORT=3000
NODE_ENV=development # Options: development, production